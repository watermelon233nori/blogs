## 为什么 C++ 中基于流的 I/O，关闭与 C 库标准 I/O 的同步以及解绑输入输出流能够加快读入和输出？

起稿：2025 年 12 月 2 日

---

其实这是个经典问题了，打过 OI 和 ACM 的应该都知道为什么，但当然我无法假设所有读者都是 OIer 和 ACMer。这个问题说实话，其实是在我理解了什么叫做刷新缓冲区才整明白的。当时我想不通的问题多了去了：

* 刷新缓冲区是什么意思？把缓冲区洗刷刷吗？

* 为什么关闭同步和解除流关联，而且这样子为什么就只能在程序结束后才能看到输出了呢？

* 为什么总有人说不建议使用 `std::endl`？（这个是在我接触算法之前看见的）

所以其实本文讨论的内容其实会有一点……宽泛，讲的东西实在是太多了，可能会有：

* Linux 的 `write` 函数
  * 什么？你问我为什么没有 Windows？因为我主要在 Linux 上琢磨的，Windows 不是很想再弄了，已放弃（

* 标准设备抽象

* 文件流重定向

* C++ 这些个 `cin`、`cout` 的流到底是什么？

* ……

总之就是大乱炖！
